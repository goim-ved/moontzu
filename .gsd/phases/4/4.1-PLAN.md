---
phase: 4
plan: 1
wave: 1
---

# Plan 4.1: Stripe Infrastructure Schema

## Objective
Update the `tenants` table to support Stripe Connect mapping for our multi-party payment routing. We will add a `stripe_account_id` field to persist the tenant's connected account identity. And we'll install necessary Stripe dependencies.

## Context
- .gsd/SPEC.md
- package.json

## Tasks

<task type="auto">
  <name>Install Dependencies</name>
  <files>package.json</files>
  <action>
    - Install the `stripe` Node.js package using `npm install stripe`.
  </action>
  <verify>npm ls stripe</verify>
  <done>Stripe package installed successfully.</done>
</task>

<task type="auto">
  <name>Create Stripe Migration</name>
  <files>supabase/migrations/0002_stripe_schema.sql</files>
  <action>
    - Create `0002_stripe_schema.sql`.
    - Alter `public.tenants` adding `stripe_account_id TEXT`.
  </action>
  <verify>Test-Path supabase/migrations/0002_stripe_schema.sql</verify>
  <done>Migration created enabling Stripe account tracking securely on the database.</done>
</task>

## Success Criteria
- [ ] Dependencies are present.
- [ ] Database is ready for Stripe Account references.
