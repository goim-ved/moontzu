---
phase: 3
plan: 2
wave: 2
---

# Plan 3.2: Tenant Admin - Service Management

## Objective
Build the internal dashboard UI for Tenant Owners and Admin to define the services they offer (e.g. "Software Consultation - 60 min", "Haircut - 30 min").

## Context
- src/app/onboarding/page.tsx
- .gsd/SPEC.md

## Tasks

<task type="auto">
  <name>Create Tenant Dashboard Layout</name>
  <files>src/app/(dashboard)/layout.tsx, src/app/(dashboard)/services/page.tsx</files>
  <action>
    - Create a generic dashboard layout protecting children routes behind authentication. 
    - The layout should identify the `tenant_id` the user belongs to (redirect to `/onboarding` if none exist).
    - Setup `src/app/(dashboard)/services/page.tsx` as the Service Management view.
  </action>
  <verify>npm run build</verify>
  <done>Protected dashboard layout restricts views to valid tenant members.</done>
</task>

<task type="auto">
  <name>Build Services UI and Actions</name>
  <files>src/app/(dashboard)/services/page.tsx, src/app/(dashboard)/services/actions.ts</files>
  <action>
    - Create `createService` server action in `actions.ts` that inserts a new service based on FormData (name, duration, price) associated with the user's `tenant_id`.
    - Build a Shadcn UI form in the services page to ingest this data.
    - Render a table or list of existing services for the tenant.
  </action>
  <verify>npm run build</verify>
  <done>Tenant services can be created securely, stored in PostgreSQL, and listed flawlessly.</done>
</task>

## Success Criteria
- [ ] Users can navigate their protected back-office dashboard.
- [ ] Service schemas are successfully hydrated with CRUD UI actions.
