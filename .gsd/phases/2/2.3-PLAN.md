---
phase: 2
plan: 3
wave: 2
---

# Plan 2.3: Branded Tenant Public Pages

## Objective
Implement dynamic rendering for the tenant's public landing page to verify that subdomain routing and database querying via the middleware parameter function correctly together.

## Context
- src/app/[domain]/page.tsx
- .gsd/SPEC.md

## Tasks

<task type="auto">
  <name>Implement Tenant Data Fetching</name>
  <files>src/app/[domain]/page.tsx</files>
  <action>
    - Ensure Page component parses the `params.domain` dynamically.
    - Query the Supabase `public.tenants` table for a tenant matching the `slug` (which is `params.domain`).
    - If no tenant is found, use the Next.js `notFound()` utility function to return a 404.
  </action>
  <verify>Get-Content src/app/`[domain`]/page.tsx | Select-String "notFound"</verify>
  <done>Dynamic route queries database and handles non-existent subdomains correctly.</done>
</task>

<task type="auto">
  <name>Build Tenant Landing UI</name>
  <files>src/app/[domain]/page.tsx</files>
  <action>
    - If a tenant is found, display a branded welcoming screen using Shadcn UI.
    - Include the Tenant's actual Name queried from the DB.
    - Add a placeholder "Book Appointment" button.
  </action>
  <verify>npm run build</verify>
  <done>UI renders the specific tenant branding ensuring full loop of multitenancy structure works.</done>
</task>

## Success Criteria
- [ ] Tenant Pages load uniquely isolated data based on the URL context.
- [ ] Unknown subdomains return a clean 404 page.
