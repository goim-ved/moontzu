---
phase: 1
plan: 1
wave: 1
---

# Plan 1.1: Project Setup

## Objective
Initialize the Next.js App Router project with Tailwind CSS, TypeScript, and Shadcn UI to establish the core frontend foundation.

## Context
- .gsd/SPEC.md
- .gsd/REQUIREMENTS.md

## Tasks

<task type="auto">
  <name>Initialize Next.js</name>
  <files>package.json, next.config.mjs</files>
  <action>
    IMPORTANT: You may need to delete the `README.md` and `.gitignore` if they conflict with `create-next-app` initialization. Or just run it so it overrides.
    - Run `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --use-npm --yes` to scaffold the project structure in the current directory.
    - After scaffolding, verify `package.json` exists.
  </action>
  <verify>npm run build</verify>
  <done>Next.js 15+ is initialized and builds successfully.</done>
</task>

<task type="auto">
  <name>Initialize Shadcn UI</name>
  <files>components.json, src/app/globals.css</files>
  <action>
    - Run `npx shadcn@latest init -d --force` to initialize shadcn/ui with default settings.
    - Run `npx shadcn@latest add button card input label form -y` to add essential UI components we will need for later Auth forms.
  </action>
  <verify>Get-ChildItem src/components/ui</verify>
  <done>Shadcn UI components are installed into src/components/ui.</done>
</task>

## Success Criteria
- [ ] Next.js project is fully scaffolded.
- [ ] Shadcn UI and core components are configured properly.
